extends Node

enum Mode {
	BRUSH,
	LINE,
	PAINT_BUCKET,
	SHAPE_SQUARE,
	SHAPE_CIRCLE,
	MOVE,
	ERASER,
	EYEDROPPER,
	SELECTION_SQUARE,
	SELECTION_FREE,
}

enum DisplayMode {
	PREVIEW,
	ACTUAL,
	NONE
}

enum SaveFileMode {
	NONE,
	OPEN_FILE, 
	NEW_FILE, 
	EXIT
}

enum FileMenuButton {
	OPEN = 0,
	SAVE,
	SAVE_AS,
	EXIT,
	NEW,
	EXPORT_PNG
}
